{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default capability for the main lesson window.",
  "windows": ["main"],
  "permissions": ["core:default", "window-state:default"]
}
